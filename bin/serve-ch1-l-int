#!/usr/bin/env bash

#
# Usage: serve-ch1-l-int [9000]
#

set -euo pipefail

port="${1:-9000}"

xdg-open "http://localhost:$port" && \
  caddy file-server --browse --listen :"$port" --root "${build:?}/ch1-l-int"
